# Expo + pnpm: hoist so Metro can see peers like @expo/metro-runtime
# even when the isolated .pnpm store would hide them.
node-linker=hoisted
shamefully-hoist=true
auto-install-peers=true
public-hoist-pattern[]=*@expo*
public-hoist-pattern[]=*expo*
